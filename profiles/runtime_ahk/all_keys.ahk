; Macropad AHK workspace
; Edit key code blocks below. Keep BEGIN/END markers intact.
; Python fallback also supports: def key1_action() .. def key12_action().

; BEGIN KEY 0,0
; END KEY 0,0

; BEGIN KEY 0,1
#Requires AutoHotkey v2.0
; Macropad runtime script for key 0,1
; Runs on key-down.
; Put your AHK commands below.
; END KEY 0,1

; BEGIN KEY 0,2
#Requires AutoHotkey v2.0
; Macropad runtime script for key 0,2
; Runs on key-down.
; Put your AHK commands below.
; END KEY 0,2

; BEGIN KEY 0,3
#Requires AutoHotkey v2.0
; Macropad runtime script for key 0,3
; Runs on key-down.
; Put your AHK commands below.
; END KEY 0,3

; BEGIN KEY 1,0
#Requires AutoHotkey v2.0
; Macropad runtime script for key 1,0
; Runs on key-down.
; Put your AHK commands below.
; END KEY 1,0

; BEGIN KEY 1,1
#Requires AutoHotkey v2.0
; Macropad runtime script for key 1,1
; Runs on key-down.
; Put your AHK commands below.
; END KEY 1,1

; BEGIN KEY 1,2
#Requires AutoHotkey v2.0
; Macropad runtime script for key 1,2
; Runs on key-down.
; Put your AHK commands below.
; END KEY 1,2

; BEGIN KEY 1,3
#Requires AutoHotkey v2.0
; Macropad runtime script for key 1,3
; Runs on key-down.
; Put your AHK commands below.
; END KEY 1,3

; BEGIN KEY 2,0
#Requires AutoHotkey v2.0
; Macropad runtime script for key 2,0
; Runs on key-down.
; Put your AHK commands below.
; END KEY 2,0

; BEGIN KEY 2,1
#Requires AutoHotkey v2.0
; Macropad runtime script for key 2,1
; Runs on key-down.
; Put your AHK commands below.
; END KEY 2,1

; BEGIN KEY 2,2
#Requires AutoHotkey v2.0
; Macropad runtime script for key 2,2
; Runs on key-down.
; Put your AHK commands below.
; END KEY 2,2

; BEGIN KEY 2,3
#Requires AutoHotkey v2.0
; Macropad runtime script for key 2,3
; Runs on key-down.
; Put your AHK commands below.
; END KEY 2,3
